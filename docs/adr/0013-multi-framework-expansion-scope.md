# 多框架扩展：最小集（React + Vue/Element Plus）

在已交付的 React 三库观感对比台之上，本批只增加 Framework-first 双级切换、Vue 真机（仅 Element Plus、场景全对齐）、React Shell + Vue Island、共享内存业务数据、以及 `/libs/:framework/:libraryId` 路由。明确仍不做更多 Vue 库、并排对比、推荐引擎、暗色/完整 i18n/后端、iframe 或微前端框架、实验室级指标与外壳 Vue 化。目标是验证多框架管道，而不是一次做完 Vue 生态目录。
